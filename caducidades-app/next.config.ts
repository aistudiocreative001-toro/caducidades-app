import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Server Actions ya están activados por defecto en Next.js 14
  // No necesitamos export estático para esta fase
};

export default nextConfig;
